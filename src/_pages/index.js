import React from 'react'
import ReactDOM from 'react-dom'
import MainHeader from '../components/HeaderComponent'

const HomePage = () =>
  <MainHeader />

ReactDOM.render(<HomePage />, document.getElementById('home-main-header'))
